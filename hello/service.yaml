apiVersion: v1
kind: Service
metadata:
  name: app1
  namespace: devel
spec:
  type: NodePort
  selector:
    app: hello
  ports:
    - name: web
      port: 80
      targetPort: web
      nodePort: 30000
